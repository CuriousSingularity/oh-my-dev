name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Generate changelog
      id: changelog
      run: |
        # Get the previous tag
        PREV_TAG=$(git describe --abbrev=0 --tags $(git rev-list --tags --skip=1 --max-count=1) 2>/dev/null || echo "")

        if [ -z "$PREV_TAG" ]; then
          # First release, get all commits
          CHANGELOG=$(git log --pretty=format:"- %s" --no-merges)
        else
          # Get commits since previous tag
          CHANGELOG=$(git log ${PREV_TAG}..HEAD --pretty=format:"- %s" --no-merges)
        fi

        # Save changelog to file
        echo "$CHANGELOG" > changelog.txt

        # Set output
        echo "changelog<<EOF" >> $GITHUB_OUTPUT
        echo "$CHANGELOG" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        body: |
          ## 🎯 Oh My Dev ${{ github.ref_name }}

          ### 📦 Installation

          ```bash
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/curioussingularity/oh-my-dev/main/tools/install.sh)"
          ```

          ### 📝 Changes

          ${{ steps.changelog.outputs.changelog }}

          ### 🔗 Links

          - [Documentation](https://github.com/curioussingularity/oh-my-dev#readme)
          - [Installation Guide](https://github.com/curioussingularity/oh-my-dev#installation)
          - [Contributing](https://github.com/curioussingularity/oh-my-dev/blob/main/.github/CONTRIBUTING.md)
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  verify-release:
    name: Verify Release Installation
    needs: create-release
    runs-on: ubuntu-latest

    steps:
    - name: Test installation from release
      run: |
        set -x  # Enable debug mode
        export OMD_DIR="$HOME/.oh-my-dev-release-test"
        export OMD_REPO="https://github.com/curioussingularity/oh-my-dev.git"
        export TERM=dumb

        # Ensure clean installation directory
        rm -rf "$OMD_DIR"

        # Download and run installer
        curl -fsSL https://raw.githubusercontent.com/curioussingularity/oh-my-dev/main/tools/install.sh | bash || {
          echo "Installation failed with exit code $?"
          exit 1
        }

        # Verify installation
        echo "Checking if $OMD_DIR exists..."
        [ -d "$OMD_DIR" ] || { echo "Error: $OMD_DIR directory not found"; exit 1; }

        echo "Checking if $OMD_DIR/oh-my-dev.sh exists..."
        [ -f "$OMD_DIR/oh-my-dev.sh" ] || { echo "Error: $OMD_DIR/oh-my-dev.sh not found"; exit 1; }

        echo "✓ Release installation verified"
